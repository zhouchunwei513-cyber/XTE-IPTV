# XTE-IPTV v5.0.3 发布：修复安装后白屏，x86 / ARM 双架构分包

> 飞牛 fnOS 上的 IPTV 直播源统一管理与智能代理。无转码、零第三方依赖，原生 Node.js，轻量稳定。

## 本次更新（v5.0.3）

- **修复部分设备安装后打开白屏**：桌面入口由内嵌 iframe 改为新窗口直连。之前通过 HTTPS / 域名访问 fnOS 时，浏览器会拦截内嵌的 `http://NAS:34500` 页面（混合内容策略），表现为点开图标一片空白；改为直连后不再受此限制。
- **按架构拆分安装包**：
  - `xte-5.0.3-x86_64.fpk` —— 64 位 x86 设备（绝大多数 Intel / AMD NAS）
  - `xte-5.0.3-aarch64.fpk` —— 64 位 ARM 设备（如 RK3588 / RK3399 等 ARM 版飞牛）
- 移除 manifest 中已废弃的 `arch` 字段，统一使用官方 `platform` 字段。
- 功能、参数、端口（默认 `34500`）与 v5.0.2 完全一致，可平滑覆盖升级。

## 下载

GitHub Release 页面：<https://github.com/zhouchunwei513-cyber/XTE-IPTV/releases/tag/v5.0.3>

直链（右键另存为 / 下载工具）：

| 架构 | 文件 | 直链 |
|------|------|------|
| x86_64（Intel/AMD） | `xte-5.0.3-x86_64.fpk` | <https://github.com/zhouchunwei513-cyber/XTE-IPTV/releases/download/v5.0.3/xte-5.0.3-x86_64.fpk> |
| aarch64（ARM） | `xte-5.0.3-aarch64.fpk` | <https://github.com/zhouchunwei513-cyber/XTE-IPTV/releases/download/v5.0.3/xte-5.0.3-aarch64.fpk> |

### 不知道自己是什么架构？

SSH 登录飞牛，执行：

```bash
uname -m
```

- 输出 `x86_64` → 下载 **x86_64** 包
- 输出 `aarch64` → 下载 **aarch64** 包

> 装错架构包会安装失败或无法启动，请先确认后再安装。

## 安装方法

1. 从上面的链接下载与你 NAS 架构对应的 `.fpk` 文件。
2. 打开飞牛 fnOS 桌面 → **应用中心** → **手动安装** → 选择下载好的 `.fpk`。
3. 按向导完成安装（默认端口 `34500`，如被占用可在安装向导中修改）。
4. 安装完成后，桌面会出现 **XTE** 图标，点击图标会在新窗口打开管理后台（本次已改为直连，解决白屏问题）。

## 从旧版本升级

直接在应用中心选择「手动安装」，用新版 `.fpk` 覆盖安装即可。已有的直播源、频道配置、会话数据均会保留（卸载才会清除）。

## 功能简介

- **集中管理 IPTV 源**：远程 URL、本地上传、文本粘贴多种导入方式；频道台标 / 名称 / 分组 / TVG-ID 可视化编辑；批量启停、分组、删除；一键导出标准 M3U（支持仅导出选中 / 当前列表 / 全部，支持内网代理、三线路合并、原始地址）。
- **智能代理，告别卡顿**：分片预取、失败重试、坏片自动跳过；三线路（内网 / IPv6 / FRP）自动适配，手机、平板、电视、电脑各类终端都能看。会话活动窗口 45s、回收 TTL 90s，覆盖电视盒子长缓冲行为。
- **无转码、零第三方依赖**：纯原生 Node.js 实现，不吃 CPU、不拖硬盘，轻量稳定。

## 常见问题

**Q：点开图标后是在浏览器新标签页打开，而不是桌面内嵌，正常吗？**
A：正常。v5.0.3 起刻意改为新窗口直连，这正是为了修复 HTTPS / 域名访问时内嵌页被拦截导致的白屏。

**Q：端口 34500 被占用了怎么办？**
A：安装向导中可以修改为其他未被占用的端口；安装后也可在应用配置里调整。

**Q：电视 / 平板还是看不了？**
A：请确认已升级到 v5.0.2 及以上版本（v5.0.3 已包含该修复）。若仍有问题，请附上后台「日志」页面的报错反馈。

## 更新日志

- **v5.0.3**：修复安装后打开白屏（桌面入口改 url 直连）；按架构拆分 x86_64 / aarch64 安装包；移除废弃 arch 字段。
- **v5.0.2**：修复电视 / 平板 / 手机等非 PC 终端不能看（会话活动窗口与回收 TTL 回退至 45s / 90s）。
- **v5.0.1**：修复 v5.0 预取分片被中断导致全终端转圈的问题。
- **v5.0.0**：智能缓冲内核、分片预取与在途复用、坏片跳过、M3U 导出等。

---

如在使用中遇到问题，欢迎跟帖反馈，并请附上：①NAS 架构（`uname -m`）②飞牛系统版本 ③后台「日志」页面的相关报错。
