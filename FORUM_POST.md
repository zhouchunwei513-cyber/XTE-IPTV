# XTE-IPTV v5.0.3 —— 飞牛 NAS 上的 IPTV 直播源管理与智能代理（开源）

> 把乱糟糟的 IPTV 直播源，变成一条全终端都能流畅看的 HLS 地址。
>
> 项目主页：https://github.com/zhouchunwei513-cyber/XTE-IPTV
> 开源协议：MIT License（免费、可自由使用与二次开发）
> 下载安装包：见下方「下载与安装包」（已区分 x86_64 与 aarch64 两个架构）

你是不是也有这样的烦恼：

- 攒了一堆 IPTV 直播源，乱七八糟没法管，台标分组全靠手搓？
- 移动的源用电信宽带看就转圈，A 省的源在 B 省就卡成 PPT？
- 手机、平板、电视、电脑各用各的源，地址换一次要改好几台设备？
- 想把整理好的频道导出成 m3u 备份 / 分享，操作繁琐？

**XTE-IPTV** 就是为解决这些问题而生的。它是一个跑在飞牛 NAS（fnOS）上的轻量代理服务，把你的所有直播源统一管理起来，对外输出标准 HLS 播放地址，全终端通用，**不转码、不吃性能、零第三方依赖、完全开源**。

---

## 一、核心功能

### 1. IPTV 源统一管理

- **三种导入方式**：远程订阅 URL、本地 m3u 文件上传、直接粘贴文本，怎么方便怎么来。
- **可视化频道编辑**：台标实时预览，名称、分组、TVG-ID 在线修改，支持搜索筛选。
- **批量操作**：勾选多个频道一键启用 / 禁用 / 分组 / 删除，管理几百个频道不费劲。
- **一键导出 M3U**：整理好的频道随时导出标准 m3u 文件，可仅导出**选中**的频道、当前列表或全部；支持内网代理地址、三线路合并、原始流地址三种形式，备份迁移分享都方便。

### 2. 解决跨运营商、跨省卡顿

这是 XTE-IPTV 的核心能力。直播源经常有「本地宽带看不卡、换个网络就转圈」的问题——源站对非本网用户限速、跨网拥塞是常态。XTE-IPTV 内置**智能缓冲内核**：

- **分片预取**：NAS 后台提前把下一片拉回来缓存好，播放器播完当前片时，下一片已经在本地等着了，命中即零等待。
- **失败重试 + 坏片跳过**：某一片回源失败自动重试，彻底失败则自动插 `EXT-X-DISCONTINUITY` 跳过，不让整个直播卡死。
- **弱网抗抖动**：客户端读不动时自动背压暂停回源，不堆内存、不爆带宽；源站抖动时指数退避，不打爆回源连接。
- **大分片友好**：4K 频道单片可达 50MB，回源超时放宽，不会误杀正在下载的大分片。

### 3. 多终端、多线路自动适配

- 一条服务同时提供**内网 / IPv6 / FRP** 三个入口，手机在外用 FRP 公网、电视在家走内网、有 IPv6 的设备直连 IPv6。
- 播放器用哪个地址连上 NAS，分片地址就自动指回哪个地址，**终端零配置**，手机 / 平板 / 智能电视 / PC 浏览器 / TVBox 全部能看。
- 内置智能缓冲与在途下载复用，桌面端内置播放器也能秒开不转圈。

### 4. 极致轻量、完全开源

- 纯 Node.js 原生 `http` 模块，**零运行时依赖**，不装 Docker、不建数据库。
- **无转码**，只做代理转发，NAS 性能占用极低。
- 代码全部开源（MIT），欢迎提 Issue、PR，也可以自己改造。
- fpk 应用包，应用中心手动安装即用，内存占用稳定。

---

## 二、下载与安装包

v5.0.3 起按架构拆分为两个安装包，请按你的 NAS 架构下载对应版本。

**先确认架构**：SSH 登录飞牛，执行 `uname -m`

- 输出 `x86_64` → 下载 x86_64 包（绝大多数 Intel / AMD NAS）
- 输出 `aarch64` → 下载 aarch64 包（ARM 设备，如 RK3588 / RK3399 等 ARM 版飞牛）

| 架构 | 安装包 | 下载地址 |
|------|--------|----------|
| x86_64（Intel/AMD） | xte-5.0.3-x86_64.fpk | https://github.com/zhouchunwei513-cyber/XTE-IPTV/releases/download/v5.0.3/xte-5.0.3-x86_64.fpk |
| aarch64（ARM） | xte-5.0.3-aarch64.fpk | https://github.com/zhouchunwei513-cyber/XTE-IPTV/releases/download/v5.0.3/xte-5.0.3-aarch64.fpk |

Release 页面（含历史版本）：https://github.com/zhouchunwei513-cyber/XTE-IPTV/releases/tag/v5.0.3

> 注意：装错架构包会安装失败或无法启动，请先执行 `uname -m` 确认后再安装。

---

## 三、安装方法

1. 按上方表格下载与你 NAS 架构对应的 `.fpk` 文件（约 121 KB）。
2. 打开 fnOS **应用中心** → 右上角 **手动安装** → 选择 fpk 文件。
3. 安装完成后打开 XTE，在「源管理」添加订阅地址 / 上传 m3u / 粘贴文本。
4. 把播放地址填进任意支持 HLS 的播放器即可观看：

| 线路 | 播放地址 |
|------|----------|
| 内网（推荐） | `http://<NAS内网IP>:34500/m3u/inner.m3u8` |
| IPv6 | `http://<NAS_IPv6>:34500/m3u/ipv6.m3u8` |
| FRP 公网 | `http://<FRP域名>:34500/m3u/frp.m3u8` |

> 小贴士：飞牛影视等客户端建议填写 NAS 的**内网地址**，避免部分终端走带宽有限的 FRP 链路造成卡顿。

兼容 VLC、PotPlayer、mpv、IINA、TVBox、IPTV Pro、TiviMate、飞牛影视等所有标准 HLS 播放器。

**从旧版本升级**：直接在应用中心「手动安装」用新版 fpk 覆盖安装即可，已有的直播源、频道配置、会话数据都会保留（卸载才会清除）。

---

## 四、v5.0.3 更新内容

- **修复**：部分设备安装后点开图标白屏。原因是桌面入口原为内嵌 iframe，当通过 HTTPS / 域名访问 fnOS 时，浏览器会拦截内嵌的 `http://NAS:34500` 页面（混合内容策略）。v5.0.3 将桌面入口改为新窗口直连，白屏问题解决。
- **新增**：按架构拆分安装包，提供 x86_64 与 aarch64 两个版本。
- **优化**：移除 manifest 中已废弃的 `arch` 字段，统一使用官方 `platform` 字段。
- 功能、参数、默认端口（34500）与此前版本完全一致，可平滑覆盖升级。

### 历次版本更新

- **v5.0.2**：修复电视 / 平板 / 手机等非 PC 终端不能看的问题。会话活动窗口与回收 TTL 回退对齐稳定值（45s / 90s），覆盖电视盒子一次性缓冲 30~60 秒才发下一请求的长缓冲行为，避免播放中会话被误回收。
- **v5.0.1**：修复 v5.0 导致所有终端转圈的致命问题。播放器请求命中后台预取分片时不再中断重启，直接复用在途下载并补发已缓冲数据；同时回退对齐稳定内核参数。
- **v5.0**：修复飞牛 PC 客户端内置播放器转圈（预取与实时流优先级反转）；新增频道导出功能（仅导出选中 / 内网代理 / 三线路合并 / 原始地址）；优化多终端并发回源调度与 4K 大分片预取策略。

---

## 五、下载与开源地址

| 项目 | 地址 |
|------|------|
| x86_64 安装包 | https://github.com/zhouchunwei513-cyber/XTE-IPTV/releases/download/v5.0.3/xte-5.0.3-x86_64.fpk |
| aarch64 安装包 | https://github.com/zhouchunwei513-cyber/XTE-IPTV/releases/download/v5.0.3/xte-5.0.3-aarch64.fpk |
| GitHub 仓库 | https://github.com/zhouchunwei513-cyber/XTE-IPTV |
| Release 页 | https://github.com/zhouchunwei513-cyber/XTE-IPTV/releases/tag/v5.0.3 |
| 开源协议 | MIT License |

---

## 六、技术说明

- **运行时**：Node.js（fnOS 内置 nodejs_v18+）
- **后端**：原生 `http` / `https`，零第三方依赖
- **前端**：原生 HTML / CSS / JS 单页管理界面
- **输出协议**：标准 HLS（m3u8 + MPEG-TS），无转码
- **开源协议**：MIT

源码结构清晰，`app/server/main.js` 为单文件后端（智能缓冲内核 / 会话缓存 / 预取容错 / API / 静态服务），`app/www/index.html` 为管理界面，欢迎 Star、Fork、提 Issue 和 PR。

---

有问题欢迎到 GitHub Issues 反馈：https://github.com/zhouchunwei513-cyber/XTE-IPTV/issues

反馈时建议附上：① NAS 架构（`uname -m` 的输出）② 飞牛系统版本 ③ 后台「日志」页面的相关报错，方便快速定位。祝大家观看愉快。
